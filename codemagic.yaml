workflows:
  react-native-dry-run:
    name: React Native Dry Run (No Build)
    max_build_duration: 10
    environment:
      node: 18.16.0
      java: 17

    scripts:
      - name: Install dependencies only
        script: |
          npm install
